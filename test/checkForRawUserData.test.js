// const checkForRawUserData = require("../main/functions/checkForRawUserData");

// describe("Checks to see if there is already card data stored. If none, saved the log file cards and say it is the first time running. If there are cards, check for any differences and return them.", () => {
//   it("Should return an object", () => {
//     expect(checkForRawUserData()).toEqual(
//       expect.any(Object)
//     );
//   });
// });